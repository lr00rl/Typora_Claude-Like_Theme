# Export-Only Sidenote Sample

这一页只保留一个最短旁注例子，方便单独验证导出行为。

导出测试<label for="sn-export-1" class="margin-toggle sidenote-number"></label><input type="checkbox" id="sn-export-1" class="margin-toggle" /><span class="sidenote">如果你在导出 HTML 或 PDF 时只想快速确认旁注浮动，可以打开这一页。</span>正文结束。
